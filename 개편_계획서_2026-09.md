# 용인 랙 견적 시스템 전면 개편 계획서

> 작성일: 2026-09-14
> 근거: 코드베이스 직접 분석(Claude) + codex 독립 리뷰 + Gemini 공식 문서 확인
> 전제: 실사용자는 아버지(현장, 60대)와 어머니(사무) 2명. 고객은 가끔 문의를 남기고 견적서를 열람하는 정도.

---

## 0. 한눈에 보기

| 구분 | 현재 | 목표 |
|---|---|---|
| 내부 화면 | 5탭 + 13개 HTML 페이지 | 3탭(견적 작성 / 업무함 / 관리) + 고객용 3페이지 분리 유지 |
| 인증 | PIN 해시를 그대로 API 토큰으로 사용, 설정 API가 비인증으로 모든 키 반환 | 서버 검증 세션 토큰, 비밀 키는 PropertiesService, 모든 관리자 API 인증 |
| AI | 요청 메모 분석 + 사진 분석, 결과를 견적에 자동 추가 | 메모 분석 제거, 사진 분석은 "제약 요소·추가 질문" 보조 도구로 축소, 자동 추가 금지 |
| Gemini 모델 | gemini-3-flash-preview (Preview) | gemini-3.5-flash-lite(텍스트) / gemini-3.6-flash(사진) 안정 버전 |
| 가독성 | 하단 탭 10px, 보조문 12px, 수량 버튼 40px | 본문 17~18px, 보조문 15px 이상, 터치 영역 48px 이상, 확대 허용 |
| 빌드 | Tailwind Play CDN, ?v=64 수동 치환 | 사전 컴파일 CSS 1장, 버전 단일 소스 스크립트 |

---

## 1. 페이지별 진단

| 페이지 | 결정 | 이유 |
|---|---|---|
| index.html | 유지, 전면 개편 | 핵심 화면. 고객 정보가 품목 뒤에 있고(204행) 종류→형태→규격→수량→추가 5단계가 김 |
| dashboard.html | 유지, "업무함"으로 개편 | 견적 목록·상태 변경은 유효. 여기에 문의를 합쳐 어머니의 단일 업무 화면으로 |
| requests.html | dashboard에 통합 | 상시 탭이 필요한 빈도가 아님. 업무함의 "새 문의" 구획으로 충분 |
| history.html | 삭제, dashboard로 리다이렉트 | 대시보드와 거의 완전 중복. 존재하지 않는 `history` 탭을 활성화하려 해 탭 강조도 안 됨(100행) |
| estimate-preview.html | 유지, 단순화 | 정식/간편/거래명세표 3탭은 과함. 견적서 + 거래명세표 2종으로 |
| view.html | 유지, 모바일 우선 재설계 | 700px A4를 통째로 축소해 모바일 글자가 너무 작음(20행, 136행) |
| request.html | 유지, 신뢰감 보강 | 업체 정보·연락 예상 시간·개인정보 안내 부족. 브랜드명이 "중용/용인 랙" 혼재 |
| more.html | 유지, "관리"로 개편 | 단가·사업자 정보·알림·앱 설치의 저빈도 허브 |
| settings.html | more.html에 통합 | GAS URL·Gemini 키까지 부모님께 노출되는 구성은 부담(264행) |
| admin-push.html | more.html에 통합 | 기기당 1회 작업. 현재 인증 가드 없음 |
| admin.html | 유지, 관리 하위 화면 | 단가 편집은 복잡·저빈도. 8~12px 글자와 32~40px 조작부 교정 필요 |
| portfolio.html | 유지, 관리 하위 화면 | 원본 사진을 리사이즈 없이 업로드해 현장 네트워크에서 실패하기 쉬움(234행) |
| portfolio-public.html | 유지 | 고객 신뢰에 기여. 외부 RSS 장애 시 대체 표시 필요(96행) |

**새 하단 탭 3개**
1. 견적 작성 (아버지 기본 시작 화면)
2. 업무함 (새 문의 → 후속 연락 필요 → 최근 견적 순)
3. 관리 (단가, 사진, 사업자 정보, 알림, 앱 설치)

---

## 2. 우선순위별 계획

작업량: S 반나절~1일 / M 2~4일 / L 1~2주

### P0. 반드시 (보안 + 데이터 안전)

**P0-1. 비밀 키 전부 교체 — S, 오늘**
- 설정 시트의 geminiApiKey, onesignalApiKey, ntfyToken이 `?action=getSettings` GET으로 누구에게나 반환되고 있음(라이브 확인 완료).
- Cloudflare 토큰 파일이 커밋 2cefced로 공개 저장소에 올라가 있음.
- git 원격 URL과 `.claude/settings.local.json`에 GitHub PAT가 평문으로 있음.
- 조치: Gemini, OneSignal, Cloudflare, GitHub PAT, ntfy 전부 폐기 후 재발급. PIN도 재설정.

**P0-2. 백엔드 인증 재설계 — M**
- `getSettings`는 회사명·전화·주소 등 공개 필드만 화이트리스트로 반환.
- 비밀 키는 설정 시트가 아니라 `PropertiesService.getScriptProperties()`로 이동.
- `doGet`의 관리자용 액션(getEstimates, getRequests, getDashboard, getSettings 전체)에 토큰 필수.
- PIN 검증을 서버에서 수행하고 성공 시 별도 세션 토큰(랜덤, 만료 30일) 발급. 저장 해시를 토큰으로 쓰지 않음.
- 서버 측 시도 횟수 제한(CacheService로 IP 또는 전역 카운트).
- `admin-push.html`에 `Auth.guard()` 추가. 현재 누구나 `role=admin` 태그를 붙여 고객 개인정보 푸시를 받을 수 있음.

**P0-3. 입력 처리 보안 — S**
- `requests.html` 172·182행, `dashboard.html` 282행, `index.html` 745행, `portfolio-public.html` 114행의 innerHTML에 `UI.escapeHtml` 적용.
- `submitRequest`에서 `=`, `+`, `-`, `@`로 시작하는 값 앞에 `'` 붙여 스프레드시트 수식 주입 차단.
- 공개 문의 API의 이미지 개수(최대 5장)·크기(장당 1.5MB)·MIME 검증을 서버에서 수행.
- 공유 토큰 생성 실패 시 견적번호 URL로 폴백하는 코드(`estimate.js` 532행)와 `view.html`의 `id` 조회 허용(98행) 제거.

**P0-4. 견적 저장 중복·유실 방지 — M**
- `clientId`를 저장 버튼마다 새로 만들지 말고 초안 시작 시 1회 생성해 draft에 보관. 현재는 타임아웃 후 재시도 시 중복 견적 발생 가능(`app.js` 1297행).
- 고객 정보 입력에도 자동 임시저장 연결(현재 품목 추가·삭제 시에만 저장, 1242행).
- 오프라인이면 "기기에 저장됨, 연결 후 다시 저장" 고정 배너.
- 저장 실패 시 입력 유지 + 큰 "다시 저장" 버튼.

**P0-5. 현장 실수 방지 규칙 — M**
- 동일 품목 재추가 시 행 추가 대신 수량 합산, 카트에서 수량 직접 수정.
- 마진은 "마진율" 한 필드로 고정해 중복 추가 방지. D/C가 총액 초과 시 차단(현재 음수 총액 가능).
- 삭제는 32px ✕ 즉시 삭제 대신 48px 버튼 + 5초 되돌리기.
- 저장 직전 "고객 / 품목·수량 / 총액 / VAT / 마진 숨김 여부" 확인 화면.
- 고객명 없으면 차단 대신 "고객 미지정으로 저장할까요?" 큰 확인창.

**P0-6. 60대 사용자 접근성 기준선 — M**
- 본문 17~18px, 보조문 15~16px, 터치 영역 48×48px 이상으로 전 페이지 통일.
- `maximum-scale=1, user-scalable=no` 제거해 확대 허용(`index.html` 5행 등 전 페이지).
- 흰 배경 위 gray-400 텍스트, 흰 글자 위 밝은 오렌지 CTA 대비 교정.
- 3초 토스트만 쓰지 말고 오류 필드 바로 아래 원인과 해결법 고정 표시, 첫 오류 필드로 스크롤·포커스.

**P0-7. AI 자동 적용 중단 — S**
- "추천 품목으로 견적 작성" 버튼을 "추천 후보 보기"로 변경. 현재 추천 랙 종류만 맞으면 단가표 첫 항목과 AI 추정 수량이 그대로 추가됨(`index.html` 773행).

### P1. 중요 (구조 + 흐름)

**P1-1. 3탭 정보구조와 업무함 통합 — L**
- `dashboard.html`에 새 문의 / 후속 연락 필요 / 최근 견적 순 배치.
- 문의·견적 모두 주 상태 1개만 사용(현재 쉼표 태그 조합은 복잡, `requests.html` 274행).
- `history.html` → dashboard 리다이렉트. `requests.html` 기능은 업무함으로 이동.
- `settings.html`, `admin-push.html`은 `more.html`의 접이식 섹션으로.
- 배지는 로컬 "총 요청 수 차이" 대신 서버의 `미처리` 건수 표시(두 기기 간 읽음 상태 불일치 해소).

**P1-2. 견적 작성 흐름 단축 — L**
- 최상단에 큰 시작 버튼 3개: 이전 견적 복사 / 자주 쓰는 세트 / 직접 선택.
- 고객 검색을 위로 이동, 고객 선택 즉시 이전 견적·사용 품목 제안(기능은 이미 있음, `index.html` 579행).
- 기기별 localStorage 빈도 대신 Google Sheet에 공용 즐겨찾기 세트 5~10개 저장. 예: "경량랙 기본 10대 + 운송비 + 마진 25%".
- 빠른 품목 클릭 시 즉시 1대 추가하지 말고 수량 먼저 묻기(`app.js` 905행).
- 최근 품목은 저장 당시 단가가 아니라 품목 ID로 현재 단가표 재조회(122행).
- 품목 추가 후 "같은 규격 계속 추가" / "다른 품목 선택" 제공(979행).

**P1-3. 고객용 견적서 모바일 우선 — M**
- 화면은 반응형 요약(업체 정보, 견적번호, 유효기간, 고객명, 총액, 포함/불포함), A4는 이미지·PDF 저장용으로만.
- 하단 4분할 버튼을 "전화로 확인" 주 CTA + 카톡·다운로드 보조로 정리.
- 견적서 안에서 다시 "견적 요청"을 유도하는 구성 제거.

**P1-4. 고객 문의 창구 신뢰감 — M**
- `request.html` 상단에 업체명 통일, 대표자·사업자번호, 실제 전화번호, 영업 지역, "영업시간 내 N시간 이내 연락".
- 연락처 수집 목적·보관 안내·동의 체크.
- 접수 후 접수번호·시각·예상 연락 시간·바로 전화/카톡 버튼.
- 관리자 기본 알림은 이미 권한이 있는 `MailApp`으로 부모님 이메일 발송. OneSignal은 인증된 기기의 보조 알림으로만.

**P1-5. AI 재설계 — M**
- `analyzeRequest` 제거. 이름·랙 종류·수량·메모는 사람이 읽는 게 더 빠르고, 현재는 수집한 치수·용도·사진조차 AI에 보내지 않음.
- 유지한다면 자유 메모를 "요구사항 요약 / 빠진 정보 / 전화로 물어볼 질문"으로 정리하는 용도만.
- `analyzePhoto`는 바닥·기둥·배관·출입문·소방설비 등 눈에 보이는 제약 후보와 추가 촬영 요청만 반환. 정확 수량·안전 판단·최종 규격은 반환하지 않음.
- 프론트는 여러 장을 보내지만 GAS는 첫 장만 사용(`Code.gs` 1599행). 각 사진을 parts로 전달하거나 UI를 1장으로 제한.

**P1-6. 빌드·캐시 정리 — M**
- Tailwind CLI로 한 번 컴파일한 정적 CSS로 교체.
- `?v=64`, `version.json`, `sw.js`의 `yr-v64`를 배포 스크립트 하나에서 생성.
- 서비스워커는 index + 필수 JS/CSS + 아이콘만 프리캐시. 현재 자산 실패를 삼켜 설치 성공처럼 처리.
- 오프라인 목표는 "단가 캐시로 견적 작성 + 초안 보존"까지. 저장·AI·사진은 온라인 필요로 명시.

**P1-7. 실제 코드 대상 테스트 — M**
- 현재 테스트는 `calc.js`를 불러오지 않고 별도 함수를 재작성하며 필드명도 `price`로 틀림(`calc.test.html` 48행).
- 최소 범위: `Calc.calcTotals`(VAT, D/C, 마진, 자유항목, 음수 방지), 재시도 시 동일 clientId, 견적 생성→미리보기→공유 1경로, 문의 접수→업무함→상태 변경 1경로.

### P2. 여유 시

- 포트폴리오 업로드: 1600px 이내 압축, 최대 10장, 장별 진행률·재시도. 공개 페이지는 RSS 실패 시 관리 포트폴리오 대체 표시. (M)
- `admin.html` 대형 인라인 스크립트와 `index.html` 사진·프리필 코드를 `js/pages/`로 분리. 프레임워크 전환 없음. (M)
- 가격 모델 판별이 프론트(`app.js` 90행)와 GAS(`Code.gs` 230행)에 중복. 시트의 `pricingModel`을 기준으로 삼고 하드코딩은 폴백만. (S)
- 시트에 "견적 작성일·공유일·문의 연결 여부"만 남겨 누락 문의 확인. 분석 대시보드는 만들지 않음. (S)
- 저장소 정리: 루트의 계획서 md는 `docs/`로, 토큰 파일 `git rm`, README에 시트 구조·설정 키·clasp 배포 절차 기록. (S)

---

## 3. Gemini 모델 권고

2026-09-14 기준 Gemini API 공식 문서 확인 결과:

| 모델 ID | 상태 | 입력/출력 ($/1M 토큰) | 비고 |
|---|---|---|---|
| gemini-3-flash-preview (현재) | Preview | 0.50 / 3.00 | Preview ID는 GA로 자동 전환되지 않아 장애 전 명시적 교체 필요 |
| gemini-3.5-flash-lite | 안정 | 0.30 / 2.50 | 가장 빠르고 저렴. 텍스트 정리·분류에 충분 |
| gemini-3.6-flash | 안정 | 0.75 / 3.75 (2026년 말까지 소개가) | 사진 입력·구조화 출력·공간 추론에 적합. 폐기 일정 페이지가 권장 대체로 명시 |
| gemini-3.8-flash | 안정 | 0.75 / 3.75 | 최신. 이 서비스엔 과함 |

**권고**
- 텍스트(메모 정리, 남긴다면): `gemini-3.5-flash-lite`
- 사진 제약 탐지: `gemini-3.6-flash`
- 모델 ID는 `Code.gs` 상수 하나로 뽑아 설정 시트 없이 코드에서만 관리.

**프롬프트·스키마 개선**
- 고객 입력은 명령이 아니라 데이터임을 명시하고 구분자로 감싸기(프롬프트 주입 방지).
- 단가표 이름 문자열 대신 불변 `catalogItemId` 후보를 반환하게 하고 서버에서 존재 검증.
- 응답 필드를 `summary`, `observedConstraints[]`, `missingQuestions[]`, `candidateItemIds[]`, `confidence`, `requiresSiteVisit`로 제한.
- 보이지 않는 항목은 반드시 `unknown` 반환.
- `responseSchema`를 실제로 API에 전달하고, 중첩 객체에도 `required`·`enum`·길이 제한 지정.
- 잘린 JSON을 정규식으로 복구하는 코드(`Code.gs` 1526행 부근) 제거. 실패 처리 또는 1회 재시도.
- AI가 반환한 수량·가격은 저장 데이터로 쓰지 않고 항상 서버 단가표로 재계산.

---

## 4. 하지 말아야 할 것

- React/Next.js나 대형 디자인 시스템으로 전면 재작성.
- Firebase·Supabase 등 별도 DB 이전, 다중 사용자 RBAC·조직·권한 체계.
- 아버지/어머니별 계정·역할. 서버가 검증하는 두 기기용 로그인이면 충분.
- AI가 사진만 보고 정확한 랙 수량·구조 안전·하중·최종 견적을 결정.
- 오프라인 자동 동기화 큐와 충돌 해결 시스템.
- 문의 상태를 CRM 수준으로 세분화, 전환 퍼널·분석 대시보드.
- 13개 페이지를 억지로 단일 SPA로 통합. 내부 3탭 + 고객용 분리가 균형점.

---

## 5. 실행 순서 제안

| 주차 | 작업 | 산출물 |
|---|---|---|
| 1주차 | P0-1 ~ P0-3 (키 교체, 인증 재설계, 입력 보안) | 안전한 백엔드, 새 PIN |
| 2주차 | P0-4 ~ P0-7 (저장 안정성, 실수 방지, 접근성, AI 자동 적용 중단) | 현장에서 믿고 쓸 수 있는 견적 화면 |
| 3~4주차 | P1-1, P1-2 (3탭 구조, 견적 흐름 단축) | 업무함, 시작 버튼 3개, 공용 즐겨찾기 |
| 5주차 | P1-3, P1-4 (고객 견적서, 문의 창구) | 모바일 견적서, 신뢰감 있는 문의 페이지 |
| 6주차 | P1-5 ~ P1-7 (AI 재설계, 빌드 정리, 테스트) | 안정 모델, 정적 CSS, 실제 테스트 |
| 이후 | P2 | 여유 시 |

각 주차 종료 시 아버지·어머니가 실제 기기에서 10분 사용해 보는 것을 검수 기준으로 삼는다.
