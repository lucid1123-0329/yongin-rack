# 용인 랙(Yongin Rack) 스마트 견적 시스템

## 프로젝트 개요

현장에서 스마트폰으로 랙 설치 견적을 산출하고 카카오톡으로 공유하는 웹앱.

## 기술 스택

- **프론트엔드:** HTML5, Tailwind CSS, Vanilla JS
- **백엔드:** Google Apps Script, Google Sheets
- **배포:** GitHub Pages, Cloudflare

## gstack

Use `/browse` from gstack for all web browsing. Never use `mcp__claude-in-chrome__*` tools.

Available skills: `/office-hours`, `/plan-ceo-review`, `/plan-eng-review`, `/review`, `/ship`, `/qa`, `/cso`, `/careful`, `/retro`, `/investigate`, `/gstack-upgrade`
